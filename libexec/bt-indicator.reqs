# bt-indicator.reqs - venv for the SNI Bluetooth tray icon (link/bin/
# bt-indicator). dbus-next is pure-Python D-Bus (BlueZ on the system bus, SNI on
# the session bus); Pillow draws the owned tile pixmaps.
dbus-next
Pillow
